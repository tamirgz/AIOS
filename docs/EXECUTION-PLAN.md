# AIOS — Recommended Execution Plan

*Written 2026-07-20; status updated 2026-07-23. The single consolidated sequence. Details live in WORKBENCH-PLAN.md (Workbench design/research) and ONE-STOP-PLAN.md (integrations, token policy, distillation strategy); this file is the order of battle.*

## Done (foundation, all shipped & verified)

M0–M10: shell + 10 modules · two-provider AI layer (Claude Max, no key + Ollama, per-job routing) · agents (cron, ledgers, approvals, live transcripts, usage panel) · semantic search over all data + Obsidian vault (local embeddings, configurable model) · Google Calendar (ICS live, API OAuth ready) · Slack in+out (connected: bot token + webhook + channels live) · external-report intakes · Ideas pipeline with AI reality-check · ops (daemons, backups, `aios` one-liner) · **two-tier memory: core blocks + archival remember/recall with provenance, consolidation agent enabled**.

**A5 — World model: project advisor + code grounding (2026-08-09).** Projects gained a **`repo_url`** (GitHub URL *or* local path): the worker keeps a **read-only copy** at `~/.aios/repos/<projectId>` — every attached repo is *copied* (`--no-hardlinks --local` for local folders), sync only ever fetches and **never pushes**, so AIOS physically cannot write the original or its origin; the copy is what agents read, and it also surfaces in the Workbench repo picker. On top of it, the **Project advisor** (P1): a `projects.readRepo` tool (recent commits + README) plus `projects.setAdvisorBrief`, and an advisor agent on **Haiku** writing a per-project read — *where it stands · the one real blocker · the next move* — shown on the cockpit with **re-read**, **→ task / → feature** (turn a recommendation into work) and an **"angle"** control that re-runs one project from a user steer. Verified live: the reads named real blockers from actual tasks/commits (hardware waits, pending sign-offs, a vague next-action), not boilerplate. Cockpit refreshes live over a new `projects_changed` SSE channel.

**Brains + reliability (2026-08-07 → 08-09).** Three additions to the AI layer and one real bug: **Codex executor** — GPT-5 (`gpt-5.6-sol`) via the **ChatGPT Pro subscription with no API key** (`codex exec --json`, `subscriptionEnv` strips `OPENAI_API_KEY` so it can never fall through to metered billing), verified end-to-end at $0; **Gemini provider** — the one deliberately metered brain (Google AI Studio key pasted in Settings, routable per job); **free-model health ledger** — the free-model catalog had no liveness signal, so a live-probe ledger + a Settings "verify free models" pass now prune retired/broken models (a full sweep found **41 of 85 dead or broken**). Reliability: the worker's LISTEN bus went **silently deaf after the Mac slept** (agent hot-reload, run/cancel, Slack intake all stopped, masked by cron) — fixed with a self-NOTIFY **heartbeat watchdog** that rebuilds and re-subscribes, verified by killing the listener backend and recovering with no process restart. Also: Workbench reports render as markdown, are editable, and clip to the Obsidian vault's `raw/` with Web-Clipper frontmatter, each carrying a **model + framework provenance line**.

**Phase 7 — "Ask" + Notion (2026-07-24).** The NotebookLM replacement. New **`ask` module**: a query box → semantic retrieval over the user's whole corpus (notes, knowledge, Obsidian vault, ideas, tasks, + Notion) → a synthesized answer that **cites its sources inline as [n]**, with clickable source cards. **Ollama-first** — a new `ask` route defaults to free `qwen3-coder:30b` (editable in Settings to escalate to Claude); answers are grounded strictly in the user's data, "I don't have that" when the corpus is silent. New **`notion` module**: a read-only, **token-gated** source — paste a Notion integration token, and pages (title + text) are mirrored into `notion_pages`, embedded by the existing sweep, and folded into `searchEverything` so Ask + search cover Notion too. Degrades gracefully with no token (a connect prompt; everything else works). Net-new = two modules + one table, reusing the whole embeddings/search spine.

**Phase 6 — Gmail read-only (2026-07-24).** New **`gmail` module** on the *same* Google OAuth — the consent scope now includes `gmail.readonly` (one re-consent grants it; calendar keeps working). A `gmail_sync` job (10 min) mirrors the last 7 days of message **metadata only** (from/subject/snippet/received/unread — never bodies) into `gmail_messages`; a Mail page (unread dots, deep links to Gmail, resync) + dashboard widget. Degrades gracefully: until re-consent, the sync returns `needsReconsent` (no crash) and the UI shows a "reconnect Google" prompt. **Feeds the loop:** a `gmail.recent` tool is wired into the **Daily-planner** and **Follow-up tracker** so mail joins the day's plan and post-meeting follow-ups. Triage stays local (the periodic agents already run free/local). Activation = one Connect-Google re-consent in Settings.

**L3 — Chief-of-staff breadth (2026-07-23).** The Life OS gains people-awareness. The Google sync now captures **attendees**; a new **`people` module** derives a light CRM from them (no manual entry) — one row per email with meeting count + first/last seen, a `people_sync` job (30 min) rebuilding it, a People page (per-person meeting history + editable notes) and dashboard widget. Follow-ups get a home: `attention_items` gained a **`personRef`**, so person-anchored cards flow into L1's "Needs you" queue. Three new **free** chief-of-staff agents: **Follow-up tracker** (`qwen3-coder:30b`, weekday evenings — proposes post-meeting follow-ups per person), **Loose-ends chaser** (`qwen3-coder:30b`, Mon/Thu — catches slipping projects/tasks/people), and **Weekly reviewer** (`gemma4:31b-it-qat`, Fri — one synthesis card + remembered priorities). Reuses the whole L1/L2 spine (attention atom, queue, tools); net-new = one table + two columns + one module.

**Free cloud provider + cloud→local fallback (2026-07-23).** Added a third agent provider — **free-tier NVIDIA cloud** (`nvidia`, OpenAI-compatible), **fail-closed to $0 models only** (a run refuses any model not confirmed free in opencode's pricing catalog; verified: a paid `deepseek-v4-flash` run was rejected, a free model ran). Agents gained a **`fallback_model`**: when a cloud primary fails on connectivity/timeout/rate-limit, the executor retries once on the agent's local Ollama model (fresh timeout, logged in the transcript) so the heartbeat survives an offline/flaky cloud. **Then benchmarked the free cloud models on both periodic agents (planner + pulse):** local `qwen3-coder:30b` beat every free NVIDIA model decisively — 4 of 6 cloud models timed out or failed on the multi-turn tool loop (free-tier latency/cold-starts), one did no work, and the one that worked (`llama-3.3-70b`) was 4–6× slower and less accurate. **Conclusion: keep the periodic agents on local `qwen3-coder:30b`;** the cloud path exists with a safe fallback but isn't the right default for a snappy heartbeat.

**L2 — Living projects (2026-07-23).** Projects stop being static folders. Each gains a `goal`, a derived `health` (on_track / at_risk / stalled / blocked) with a one-line reason, and a `lastActivityAt` computed on read across everything linked by `projectRef` (tasks, notes, attention). New **project cockpit**: the grid shows a health chip + next-action + last-active per card; the detail page has a cockpit header (goal & next-action inline-editable, health + reason, rollup stats, and the project's own "Needs you" cards). Health is **never blank** — a deterministic read-time heuristic fills it with zero agent runs, and the **Project-pulse agent** (free local model, 07:00 weekdays) refines it, fills a missing goal/next-action, and raises a card *only* for stalled/blocked projects (idempotent per ISO week). The agent's writes never touch `updatedAt`, so assessing a project can't reset its own stall clock. **Model chosen empirically (2026-07-23):** the exact pulse task was benched across all 12 tool-capable Ollama models, then the top 4 re-run 3× each for consistency. **`qwen3-coder:30b` won decisively** (score 94/100, range 87–97, ~42s) — the only model that repeatedly set correct health on *every* project **and** reliably raised the stall/blocker cards; `qwen3:8b` scored 42 (slow, erratic — one run skipped health entirely). Pulse now runs on `qwen3-coder:30b`; the daily-planner stays on `qwen3:8b` (different workload, not re-benched).

**L1 — Attention spine (2026-07-23).** First slice of the Life OS. New `today` module: the `attention_items` atom + `nextAction` on projects; the **Today** command surface = Plan-my-day (calendar + due tasks + project next-actions, with schedule-to-calendar) + the **"Needs you" queue** that *aggregates* attention items + approvals + Workbench needs_input (not a fourth inbox). The **Daily-planner** agent runs on a **free** local model (qwen3:8b) — `AgentTemplate` now carries `defaultProvider/defaultModel` so the heartbeat can never bill. Verified end-to-end live. W3 delegation-UX is absorbed (its needs_input→Inbox *is* this spine).

**W2 — Executor breadth (2026-07-22 → 23, exit test passed; free cloud verified live 2026-07-23).** Generic CLI adapter: any coding agent is a config row (command template, parser, timeout), not code. opencode/pi/aider seeded; `code-local` task type; per-task executor+model override; Settings panel. Honesty hardened: an executor that prints nothing, or changes no files, is reported as failed, not done. CLI executors run in a local **clone** (real `.git`, correct project root), and `PWD` is pinned to the isolated dir — without which opencode edited the AIOS checkout itself. **Free-model selection** across local Ollama + free cloud (opencode-zen Big Pickle, free Nvidia models), cost-verified from opencode's pricing DB so paid models are refused before spawn; catalog built by file read (pages ~0.01s). **Verified live, all $0:** Big Pickle finished a real edit in 24s, a free Nvidia model (minimax-m2.7) in 133s — both through the Workbench, diffs spec-checked. Root-caused and fixed opencode's startup hang (missing `@ai-sdk/openai-compatible` → `bun install` on every run). **W2 remaining:** pi runs but its JSON parser needs rebuilding against a real fixture; aider is not installed; pi/aider cloud tiers deferred. Detail in WORKBENCH-PLAN §7–§8.

**W1 — Workbench spine (2026-07-21).** 11th module. One-box delegation → executor runs unattended → review in-app. Task/attempt/event schema; supervision engine (git worktree per attempt, per-type timeouts, SIGTERM→SIGKILL on the process group, restart reconciliation, capacity cap 2); claude-headless + native adapters; board + detail with live tail and per-file diff; `workbench.delegate/list/get` tools. Zero worker-code edits — two registry lines, as promised.

## Sequence

| # | Phase | Contents | Exit test | Status |
|---|---|---|---|---|
| 0 | **Activation** | GCP OAuth client → Settings; Slack bot token + channels; Slack webhook | Calendar shows event colors; #tldr/#my-today in External Reports; bell reaches Slack | ✅ **DONE (2026-08-10)** — Google (OAuth connected, 119 events, real colors, meeting links) **and** Slack both live: bot token + webhook + report channels + inbox channels all set; per-channel sync cursors advancing; `bell→Slack` verified (delivered a real notification) |
| 1 | **W1 — Workbench spine** | Task/attempt/event schema; supervision engine (worktrees, timeouts, SIGTERM-safe, reconciliation); claude-headless + native adapters; cards + drawer UI (live tail, diff) | Delegate "research X" and "fix this bug" from one text box; both finish unattended; diff reviewed in-app | ✅ **Shipped 2026-07-21** — exit test passed on the real system (see WORKBENCH-PLAN §6) |
| 2 | **W2 — Executor breadth** | Generic CLI adapter (template + parser + timeout); opencode/pi/aider as executor **rows**; `code-local` type; per-task executor override; executors panel in Settings | Same coding task run on Claude and opencode+qwen as sibling attempts, compared by diff | ✅ **PASSED 2026-07-23** — one card, Claude Sonnet (14s/$0.15) + opencode·qwen3.5-coder (87s/**$0**), both correct diffs reviewed in-app. Two root-caused bugs fixed (stale PWD; linked-worktree → clone). See WORKBENCH-PLAN §7 |
| 3 | **L1 — Attention spine** | `attention_items` atom; **"Needs you" queue** (aggregates items + approvals + Workbench `needs_input`); `nextAction` on projects; **Plan-my-day** (→ calendar blocks); **Daily planner** agent on `qwen3:8b`; trust gradient | A week of mornings where a 30-sec approve set the day, and every "needs you" moment lived in one queue | ✅ **SHIPPED 2026-07-23** — new `today` module. Verified live: dedupe works; queue unifies all 3 sources; the daily-planner ran on qwen3:8b (free, 74s/$0) and raised typed project-anchored cards; inline resolve persists. Pages ~0.01s |
| 4 | **L2 — Living projects** | Projects gain `goal`/`health`(derived)/`lastActivityAt`; **project cockpit** rollup via existing `projectRef`; **Project pulse** agent (`qwen3-coder:30b`, benched-best) → health + next-action + stall/blocker cards | Every active project shows health + next action without you setting either | ✅ **SHIPPED 2026-07-23** — cockpit live: all active projects show a health with **zero agent runs** (read-time heuristic), grid + detail cockpit render, inline goal/next-action editors persist. Pulse ran on qwen3:8b ($0, 81s): read the world, set health on all 3, correctly raised **no** card for at-risk. Caveat: 8b goal-*invention* is weak → agent left **manual**, not scheduled |
| 5 | **L3 — Chief-of-staff breadth** | People/follow-ups (`people` table off calendar attendees) + **Follow-up tracker**; **Loose-ends chaser**; **Weekly reviewer** (`gemma4:31b-it-qat`) | A week where AIOS chased loose ends & prepped follow-ups before you noticed | ✅ **SHIPPED 2026-07-23** — `people` module (12 people from real calendar), `personRef` follow-ups into the queue, 3 free agents scheduled. Follow-up tracker verified live ($0, 32s) raising a person-anchored card. Connector deferred |
| 6 | **Gmail + local rerouting** *(feeds L3 + Plan-my-day)* | Gmail read-only module (same OAuth); emails join the follow-up tracker and the daily plan; triage stays local | A morning where the plan made opening Gmail/Calendar unnecessary; >80 % of AI calls local | ✅ **SHIPPED 2026-07-24** — `mail` module (metadata-only sync, 10-min job, page + widget), `gmail.recent` wired into planner + follow-up tracker, graceful degradation verified. **Activation: one Connect-Google re-consent** (scope added; token needs refresh) |
| 7 | **"Ask" + Notion** | Notion read-only index; Ask page — cited Q&A over knowledge/vault/notion, Ollama-first with escalate | 3 NotebookLM-grade questions answered with citations, zero Claude tokens | ✅ **SHIPPED 2026-07-24** — `ask` module (cited Q&A, Ollama-first `qwen3-coder:30b`) + `notion` module (token-gated read-only source folded into search). Notion activates on a token |

### The Agentic-OS layers *(roadmap reconstructed 2026-08-09 — supersedes old phases 8–9 and the P1–P6 project list)*

Horizontal OS layers; **projects/work are the primary object they operate on**, but each layer also serves people, calendar, ideas, knowledge and ops. Loop: **sense → decide → act → verify → report.**

| # | Layer | Contents *(absorbs)* | Exit test | Status |
|---|---|---|---|---|
| A1 | **Work Kernel — routines** | The unit of autonomous work: `trigger + steps + brain + success-check + destination`. Projects: watch a repo, draft status, chase blockers. Beyond: vault ingest, digests, recurring ops *(absorbs "Routines engine", P3)* | One dull recurring task of yours runs end-to-end, unattended, for a week | ✅ **first-class Routine object shipped 2026-08-11** — `routines` table + panel on the Workbench: a standing ask + repo + trigger (**new commit** via HEAD-poll with a compare-and-swap ledger, and/or **cron**) + brain. Fires → spawns a Workbench task → judge verifies → **destination = approval-gated PR** (never a direct write). Commit trigger is idempotent (CAS on `lastSeenSha`). *Remaining:* turn-budget for many-item runs; the earlier Repo-watcher digest routine still runs alongside |
| A2 | **Trust & permissions** | Verification ("done" = a real check passed) + approval queue for every outward act (email/Slack/calendar). The syscall-permission layer *(absorbs "Trust layer", P5)* | An agent drafts a real reply → you approve → it sends; and a failed check blocks a false "done" | ✅ **SHIPPED 2026-08-10** — both halves: (1) an agent `successTool` gate — a run is `failed` unless that tool actually succeeded (proven both ways); (2) the outward-action **approval queue** — `slack.post` (first outward action) is `risk:"approval"`, so an agent *drafts* → it parks in "Needs you" → **approve/reject inline** → the worker runs (or drops) it. Verified end-to-end (queue=pending/nothing sent · reject=no-op · approve=tool runs & reaches Slack). *Remaining:* more outward tools (email draft, calendar) on the same spine |
| A3 | **Senses — ingest & route** | Everything flows in and lands where it belongs: `#tldr`/`#my-today` routed to the project they affect; email triage + approval-gated drafts; files *(absorbs P2, two-way email, desktop capture)* | A week of `#tldr` lands as per-project signal; inbox triaged without opening Gmail | ⬜ |
| A4 | **Attention scheduler** | AIOS pings **you** — stalls, deadlines, waiting-triggers, decisions needed *(absorbs "proactive attention routing", P5)* | A stalled project reached you before you noticed it | 🟡 attention items + queue exist |
| A5 | **World model — grounding** | Deep current context that makes advice real. Extend advisor to people, decisions, knowledge; proactive recall *(absorbs "grounding & code context", P1)* | An advisor read cites the right note/decision/commit unprompted | ✅ **advisor + code repos shipped 2026-08-09**; breadth pending |
| A6 | **Deliverable factory** | Workbench → real work products (PRD, analysis, code change, report), repo-grounded and verified — **incl. the merge/PR button** so an approved branch actually lands *(absorbs P4 + the W3 merge remnant)* | A delegated deliverable lands good enough to use as-is — **and gets merged from inside AIOS** | 🟡 Workbench + repo grounding + judge done; **PR delivery shipped 2026-08-11** — `workbench.openPR` (push branch + `gh pr create`, resolves cache→local→GitHub), approval-gated (draft→approve→open), on both routine and manual tasks. *Remaining:* AIOS never merges (by design — the PR is the hand-off) |
| A7 | **Brain-trust router + governor** | Per-task routing across Claude / GPT-5 (Codex) / Gemini / free-local; multi-opinion on the hardest calls; quota + cost ceilings *(absorbs "brain-trust router")* | A month with no quota surprise; the hard call got two opinions | 🟡 4 brains wired; routing + governor missing |
| A8 | **Control room** | What ran · running · cost · pending your approval · failed — per project and system-wide *(absorbs "control room", P6)* | You can answer "what did AIOS do this week?" at a glance | ⬜ |
| A9 | **Reliability spine** | Always-on: heartbeat, catch-up on missed runs, idempotency ledgers, self-healing | A sleep/restart cycle loses no work | 🟡 listener heartbeat shipped 2026-08-09 |

**Removed — decided against (do not rebuild):**
- **Morning Brief / daily-brief notification** — Claude Desktop already posts the briefing to **#my-today** and the TLDR to **#tldr**. AIOS **consumes** those channels (A3) instead of duplicating them. This also retires "Daily brief v2" from the old Phase 6.
- **OpenAI as a routing provider** — GPT-5 stays the **Codex Workbench executor** (ChatGPT subscription, no API key). A metered OpenAI key is explicitly not wanted; the reasoning is pinned in a Settings tooltip.
- **Multi-agent framework sidecar** (CrewAI / Agno / Langflow / Pydantic-AI) — researched 2026-08-09: orchestration adds failure surface, not capability, and no free local model handled the judgment task. Revisit only if a task genuinely decomposes.

**Parked — deferred, not killed:** phone reach (PWA/Telegram + Tailscale) and mobile/share-sheet capture. ⌘K already covers desktop command; reach returns when there is more worth reaching for.

#### Full-backlog audit *(2026-08-09)* — everything from the original plan that A1–A9 did **not** carry forward

A first pass claimed "nothing else was lost"; auditing the original plan + WORKBENCH/ONE-STOP tails proved that wrong. Complete ledger:

| Item | Origin | Disposition |
|---|---|---|
| **Workbench merge / PR button** | W3 remnant — "only the merge/PR button stays a Workbench task" | ✅ **Shipped 2026-08-11** as approval-gated PR delivery (`workbench.openPR`): push branch + `gh pr create`, on routine and manual tasks. AIOS opens the PR (on your approval) but never merges — the PR is the hand-off |
| **Habits / Metrics tracker** | Original plan, future module #5 | ❌ **Dropped** — a widget-system demo, not project/work leverage |
| **CRM / Sales module** | Original plan, future module #3 ("the deferred module") | ❌ **Dropped as a module** — the `people` CRM-lite (calendar-derived, follow-ups, personRef) already covers the project-ops need; a sales pipeline is out of scope |
| **`pi` JSON parser rebuild · `aider` install · pi/aider free-cloud tiers** | W2 tails | ❌ **Dropped** — `opencode` (free local + free cloud), `claude-headless` and `codex` cover every executor need; two unproven rows aren't worth maintaining |
| **Connector agent** ("this item relates to project X — attach it?") | L3 §3.4, deferred optional | ✅ **Absorbed into A3** — proactive item→project routing *is* A3's core |
| **Auth gate** | Old Phase 9 | ⏸️ **Parked with phone reach** — AIOS is localhost-only today; the gate becomes mandatory the moment it's exposed (ships together with Tailscale/PWA, never after) |
| **Notes-vs-Vault decision · module graveyards · fluidity pass · agent 👍/👎 evals** | Old Phase 9 | ⏸️ **Parked** — the "subtract" pass, worth doing after A1–A3 |

## What AIOS is — an Agentic OS with projects at the centre *(reframed 2026-08-09)*

L1–L3 built the **attention loop** (living projects → chief-of-staff agents → one typed attention atom → "Needs you" queue / project cockpit / Plan-my-day, under a trust gradient). That stands. What changed is the frame above it:

**AIOS is an agentic OS whose primary object is a project** (work *and* personal projects). The A-layers are horizontal — a kernel for autonomous work (A1), a permission layer (A2), senses (A3), a scheduler for your attention (A4), a world model (A5), an output factory (A6), a brain scheduler (A7), observability (A8) and an always-on spine (A9). Projects are what those layers mostly act on, but the same layers serve people, calendar, ideas, knowledge and ops — which is what keeps it an OS rather than a project tracker.

It is **not** a life-brief generator: the daily brief and TLDR come from Claude Desktop into Slack, and AIOS consumes them (A3). Full design in **ONE-STOP-PLAN §3**; the old "W3 delegation UX" folds into L1's spine.

## Standing principles

- **The heartbeat runs free.** Every periodic/scheduled agent uses a **free model** — benchmarked 2026-07-23, **`qwen3-coder:30b` (local) is the default for the planner + pulse** (correct health + card discipline, ~20-50s; beat both `qwen3:8b` and every free NVIDIA cloud model). `gemma4:31b-it-qat` for the weekly synthesis. A **free-tier NVIDIA cloud** provider exists (fail-closed to $0 models) with a **cloud→local fallback**, but local is the default — the free cloud tier was too slow/unreliable for a heartbeat. Claude is only for one-off, on-demand judgment; the metered-key guard forbids anything paid.
- **Observe, don't ask to be fed.** Derive health, next-actions, follow-ups and loose ends from calendar/email/git/notes and the *absence* of activity — ask only for decisions.
- **Memory is load-bearing**: blocks stay terse (budgets enforced); everything durable goes to archival via `memory.remember`; agents recall before re-deciding; the Sunday consolidation agent keeps both fresh. Nothing important lives only in a chat transcript.
- **Volume, summarization & the heartbeat → local Ollama; one-off judgment & action → Claude Max.**
- **Merge is manual, always.** Agent work stays on its branch until you click.
- **Every phase independently useful; every exit test runs in the real system.**
- Operational: `aios` to start (prod default); migrate ⇒ restart web+worker; never rebuild under a running server; backups nightly at 03:30.
